import { StyleSheet } from 'react-native';

const inputDialogStyles = StyleSheet.create({
    dialogContent: {
        marginTop: 20,
        width: 350,
    }
});

module.exports = inputDialogStyles;